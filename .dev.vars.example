# Local Wrangler development secrets (do not commit real values)
ENQUIRY_WEBHOOK_SECRET="replace-with-local-dev-secret"

# Public endpoint used by local/static builds
PUBLIC_ENQUIRY_ENDPOINT="https://script.google.com/macros/s/REPLACE_WITH_YOUR_DEPLOYMENT_ID/exec"
