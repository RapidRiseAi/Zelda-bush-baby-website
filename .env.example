# Public URL used by the front-end form submit.
PUBLIC_ENQUIRY_ENDPOINT="https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec"

# Secret should be injected by your server/proxy (not directly from browser JS).
ENQUIRY_WEBHOOK_SECRET="replace-with-long-random-secret"
