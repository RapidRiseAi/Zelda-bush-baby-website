{
  "name": "zelda-bush-baby-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "npm run build && wrangler deploy",
    "preview:worker": "npm run build && wrangler dev"
  },
  "dependencies": {
    "astro": "^5.5.0"
  },
  "devDependencies": {
    "wrangler": "^4.75.0"
  }
}
